---
import SectionHeader from '../ui/SectionHeader.astro';
import Reveal from '../ui/Reveal.astro';
import Button from '../ui/Button.astro';
import InsightCard, { type Props as InsightCardProps } from '../ui/InsightCard.astro';

export interface Props {
  items: readonly InsightCardProps[];
}

const { items } = Astro.props;
---

{/* Omitted when nothing is published — an empty blog preview is worse than none. */}
{
  items.length > 0 && (
    <section class="section section--alt" aria-labelledby="insights-title">
      <div class="shell">
        <Reveal>
          <SectionHeader id="insights-title" eyebrow="Insights" title="Ideas, Experiments & Lessons">
            <p>
              I share practical thoughts on technology, digital marketing, business, and the lessons
              that come from actually building things.
            </p>
          </SectionHeader>
        </Reveal>

        <Reveal as="ul" stagger class="grid" role="list">
          {items.map((item) => (
            <li>
              <InsightCard {...item} />
            </li>
          ))}
        </Reveal>

        <div class="more">
          <Button href="/insights" variant="ghost">Read All Insights</Button>
        </div>
      </div>
    </section>
  )
}

<style>
  .grid {
    display: grid;
    gap: 2rem;
  }

  @media (width >= 48rem) {
    .grid {
      grid-template-columns: repeat(3, minmax(0, 1fr));
    }
  }

  .more {
    margin-top: 2rem;
  }
</style>
